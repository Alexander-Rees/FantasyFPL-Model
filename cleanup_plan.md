# Cleanup plan
1. Remove duplicate model-files copy/
2. Remove FPL-Elo-Insights/ (can be cloned when needed)
3. Remove venv/ (can be recreated)
4. Remove __pycache__/
5. Remove frontend build/ and node_modules/
6. Remove backend target/
7. Add proper .gitignore entries
